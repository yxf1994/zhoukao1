#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../angular-g/bin/angular-app" "$@"
  ret=$?
else 
  node  "$basedir/../angular-g/bin/angular-app" "$@"
  ret=$?
fi
exit $ret
